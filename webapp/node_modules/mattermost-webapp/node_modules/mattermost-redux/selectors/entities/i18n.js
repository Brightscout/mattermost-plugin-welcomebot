"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getCurrentUserLocale = getCurrentUserLocale;

var _common = require("./common");

var _constants = require("../../constants");

// Copyright (c) 2015-present Mattermost, Inc. All Rights Reserved.
// See LICENSE.txt for license information.
function getCurrentUserLocale(state, defaultLocale = _constants.General.DEFAULT_LOCALE) {
  const currentUser = (0, _common.getCurrentUser)(state);

  if (!currentUser) {
    return defaultLocale;
  }

  return currentUser.locale || defaultLocale;
}