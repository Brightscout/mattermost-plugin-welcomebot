import { GlobalState } from "../../types/store";
export declare function makeGetUsersTypingByChannelAndPost(): (state: GlobalState, props: {
    channelId: string;
    postId: string;
}) => Array<string>;
export declare const getUsersTyping: (state: GlobalState) => Array<string>;
