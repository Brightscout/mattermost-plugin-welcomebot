import { Action, Reducer } from "../types/actions";
export declare const offlineConfig: {
    effect: (effect: Function, action: Action) => any;
    discard: (error: Error, action: Action, retries: number) => boolean;
};
export declare function createReducer(baseState: any, ...reducers: Reducer[]): Reducer<{}, Action>;
